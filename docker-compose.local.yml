services:
  allbot:
    build: .
    image: sxkiss/allbot:local
    container_name: allbot
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./:/app # 本地开发：挂载整个目录
      - redis_data:/data/redis

volumes:
  redis_data:
    name: redis_data
